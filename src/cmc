#!/bin/bash
# Wrapper for cmc

call=$1
DIR="/usr/share/cmc/prog"

if [ "$call" == "--compile" ]; then
	python $DIR/build.pyc
elif [ "$call" == "--sync" ]; then
	python $DIR/sync.pyc
else
	python $DIR/cmcompiler.pyc
fi
